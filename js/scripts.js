$(document).ready(function() {
  $("#submit").click(function() {

    var number = parseInt($("#number").val());
    var array =[];
    $("#response").text(array);

    function beepboop(number){
      for (i = 0; i <= number; i++){
      }array.push(i)
    }
  });
});
